# FastMCP — v1 API официального MCP SDK; в mcp 2.x переименован в MCPServer
# с ломающими изменениями — верхний пин осознанный.
#
# Нижняя граница — не «лишь бы импортировалось»: у mcp 1.9.4 восемь известных
# CVE (транспорты HTTP/WebSocket не проверяют Host/Origin и принципала сессии),
# и прежняя граница >=1.2.0 их разрешала. У 1.30.0 известных уязвимостей нет
# (OSV, проверено 22.09.2026), requires-python тот же >=3.10, FastMCP v1 API
# на месте. Гейт — задание osv в .github/workflows/supply-chain.yml.
mcp>=1.30.0,<3

# Транзитивные зависимости самого SDK, у которых он объявляет слишком низкий
# минимум: mcp 1.30.0 просит python-multipart>=0.0.9, а у 0.0.9 шестнадцать
# известных CVE. pip обычно ставит свежую версию, и вживую этого не видно —
# но файл РАЗРЕШАЕТ уязвимую, и резолвер, предпочитающий минимумы (в том числе
# сканер OSV), её и возьмёт. Границы ниже говорят вслух то, что и так
# происходит: версии без известных уязвимостей на 22.09.2026.
#
# Верхних границ здесь намеренно нет: это не наши прямые зависимости, и
# запирать чужое дерево сверху значит ломать установку при первом же мажоре SDK.
anyio>=4.10.0
idna>=3.10
python-multipart>=0.0.20
