# scripts/fetch_counterparty.py — боевой скрипт, идущий из живой эксплуатации.
# Осознанно выключено:
#   UP031 (% -форматирование) — стиль скрипта, единообразие сообщений об ошибках
#   BLE001, S110, S112 (широкие except) — сетевые вызовы к недокументированным
#     эндпоинтам ФНС: любой сбой транспорта превращается в «недоступен», а не в traceback
#   RUF059 (неиспользуемые распаковки) — статус HTTP читается в отладке
[lint]
ignore = ["UP031", "BLE001", "S110", "S112", "RUF059"]
